package dev.kafkalearn.patterns;

/**
 * ЭТАП 11 — ЗАГОТОВКА. Идемпотентный консьюмер.
 *
 * Проблема: at-least-once означает, что сообщение может прийти дважды
 * (например, после ребалансировки до коммита оффсета). Если обработка
 * не идемпотентна — получим двойное списание, двойной email и т.п.
 *
 * Подходы к реализации:
 *   A) Дедуп по бизнес-ключу события (eventId) в персистентном хранилище
 *      с TTL. Плюс: просто. Минус: нужно внешнее хранилище.
 *   B) Идемпотентная запись в приёмник: UPSERT по первичному ключу
 *      вместо INSERT. Плюс: без доп. состояния. Минус: не для всех операций.
 *   C) Условная запись с версией: обновляем только если version > current.
 *
 * Задачи:
 *   [ ] 1. Реализовать вариант A с in-memory LRU-кешем + окном дедупа.
 *   [ ] 2. Смоделировать дубли (перечитать партицию с прошлого оффсета)
 *          и доказать, что побочный эффект произошёл ровно один раз.
 *   [ ] 3. Обосновать в NOTES.md выбор размера окна дедупа.
 *
 * Вопрос: почему одного in-memory кеша недостаточно в продакшене?
 */
public class IdempotentConsumer {

    public static void main(String[] args) {
        throw new UnsupportedOperationException("ЭТАП 11: реализуйте самостоятельно");
    }
}
